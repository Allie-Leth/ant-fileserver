[pytest]
addopts = -ra
testpaths =
    tests/unit
    tests/api
    tests/
markers =
    k8s: Kubernetes validation tests
    integration: Integration tests requiring external services
    slow: Tests that take longer to run
